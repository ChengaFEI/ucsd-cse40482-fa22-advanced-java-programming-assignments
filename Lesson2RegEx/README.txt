Lesson2RegEx Console Application

Usage: Enter "neighbor-dump.txt" and 2 PAN IDs, 4 MAC addresses and 4 RF_RSSI_M values for MAC addresses will be printed out on console.

Alert: If text file name is entered incorrectly or missing, this console application won't work.